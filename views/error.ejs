<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - HR Management System</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #2563eb;
            --primary-dark: #1d4ed8;
            --secondary-color: #64748b;
            --success-color: #059669;
            --error-color: #dc2626;
            --error-light: #ef4444;
            --error-bg: #fef2f2;
            --background-color: #f8fafc;
            --surface-color: #ffffff;
            --border-color: #e2e8f0;
            --border-light: #f1f5f9;
            --text-primary: #0f172a;
            --text-secondary: #475569;
            --text-muted: #64748b;
            --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
            --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            --radius-sm: 0.375rem;
            --radius-md: 0.5rem;
            --radius-lg: 0.75rem;
        }

        * {
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background-color: var(--background-color);
            color: var(--text-primary);
            line-height: 1.6;
            margin: 0;
            min-height: 100vh;
        }

        .main-container {
            max-width: 480px;
            margin: 0 auto;
            padding: 2rem 1rem;
        }

        /* Header Section */
        .header {
            text-align: center;
            margin-bottom: 2.5rem;
        }

        .company-logo {
            width: 48px;
            height: 48px;
            background: var(--error-color);
            border-radius: var(--radius-lg);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1rem;
            box-shadow: var(--shadow-md);
            animation: shake 0.8s ease-in-out;
        }

        @keyframes shake {

            0%,
            100% {
                transform: translateX(0);
            }

            10%,
            30%,
            50%,
            70%,
            90% {
                transform: translateX(-3px);
            }

            20%,
            40%,
            60%,
            80% {
                transform: translateX(3px);
            }
        }

        .company-logo i {
            color: white;
            font-size: 1.5rem;
        }

        .page-title {
            font-size: 1.875rem;
            font-weight: 700;
            color: var(--text-primary);
            margin: 0 0 0.5rem 0;
            letter-spacing: -0.025em;
        }

        .page-subtitle {
            font-size: 1rem;
            color: var(--text-secondary);
            margin: 0;
            font-weight: 400;
        }

        /* Error Card */
        .form-card {
            background: var(--surface-color);
            border: 1px solid var(--border-color);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-lg);
            padding: 2rem;
            transition: box-shadow 0.2s ease;
            border-top: 3px solid var(--error-color);
        }

        .form-card:hover {
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }

        /* Error Message Section */
        .error-section {
            margin-bottom: 1.5rem;
        }

        .error-title {
            font-size: 1.25rem;
            font-weight: 600;
            color: var(--text-primary);
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .error-title i {
            color: var(--error-color);
            width: 20px;
            text-align: center;
        }

        .error-message {
            background: var(--error-bg);
            border: 1px solid rgba(220, 38, 38, 0.2);
            border-left: 4px solid var(--error-color);
            border-radius: var(--radius-md);
            padding: 1rem;
            color: var(--text-secondary);
            font-size: 1rem;
            line-height: 1.5;
            margin-bottom: 1.5rem;
        }

        /* Help Section */
        .help-section {
            background: #f8fafc;
            border: 1px solid var(--border-light);
            border-radius: var(--radius-md);
            padding: 1.25rem;
            margin-bottom: 1.5rem;
        }

        .help-title {
            font-size: 0.875rem;
            font-weight: 600;
            color: var(--text-primary);
            margin-bottom: 0.75rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            letter-spacing: 0.025em;
        }

        .help-title i {
            color: var(--primary-color);
            width: 16px;
            text-align: center;
        }

        .help-list {
            margin: 0;
            padding: 0;
            list-style: none;
        }

        .help-item {
            display: flex;
            align-items: flex-start;
            gap: 0.75rem;
            margin-bottom: 0.5rem;
            font-size: 0.8125rem;
            color: var(--text-muted);
            line-height: 1.25rem;
        }

        .help-item:last-child {
            margin-bottom: 0;
        }

        .help-item::before {
            content: '•';
            color: var(--primary-color);
            font-weight: bold;
            width: 4px;
        }

        /* Button */
        .btn-primary {
            width: 100%;
            background-color: var(--primary-color);
            border-color: var(--primary-color);
            color: white;
            font-weight: 600;
            font-size: 1rem;
            padding: 0.875rem 1.5rem;
            border-radius: var(--radius-md);
            border: 1px solid transparent;
            transition: all 0.2s ease;
            text-align: center;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            position: relative;
            overflow: hidden;
        }

        .btn-primary:hover {
            background-color: var(--primary-dark);
            border-color: var(--primary-dark);
            transform: translateY(-1px);
            box-shadow: var(--shadow-lg);
            color: white;
        }

        .btn-primary:active {
            transform: translateY(0);
            box-shadow: var(--shadow-md);
        }

        .btn-primary:focus {
            box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.25);
            color: white;
        }

        /* Footer */
        .footer {
            text-align: center;
            margin-top: 2rem;
            padding-top: 1.5rem;
            border-top: 1px solid var(--border-light);
        }

        .footer-text {
            font-size: 0.8125rem;
            color: var(--text-muted);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.375rem;
        }

        .security-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.375rem;
            font-size: 0.75rem;
            color: var(--error-color);
            background-color: var(--error-bg);
            padding: 0.25rem 0.5rem;
            border-radius: var(--radius-sm);
            margin-top: 0.75rem;
        }

        /* Responsive Design */
        @media (max-width: 640px) {
            .main-container {
                padding: 1.5rem 1rem;
            }

            .form-card {
                padding: 1.5rem;
            }

            .page-title {
                font-size: 1.5rem;
            }
        }

        /* Focus Management */
        .btn-primary:focus-visible {
            outline: 2px solid var(--primary-color);
            outline-offset: 2px;
        }

        /* Animation for smooth transitions */
        * {
            transition-property: color, background-color, border-color, text-decoration-color, fill, stroke, opacity, box-shadow, transform, filter, backdrop-filter;
            transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
            transition-duration: 150ms;
        }

        /* Professional spacing */
        .form-card>*:last-child {
            margin-bottom: 0;
        }

        /* Improved typography */
        .error-title {
            letter-spacing: 0.025em;
        }
    </style>
</head>

<body>
    <div class="main-container">
        <!-- Header -->
        <div class="header">
            <div class="company-logo">
                <i class="fas fa-exclamation-triangle"></i>
            </div>
            <h1 class="page-title">Error Occurred</h1>
            <p class="page-subtitle">HR Management System</p>
        </div>

        <!-- Error Card -->
        <div class="form-card">
            <!-- Error Section -->
            <div class="error-section">
                <div class="error-title">
                    <i class="fas fa-times-circle"></i>
                    Something went wrong
                </div>

                <div class="error-message">
                    <%= message %>
                </div>

                <!-- Help Section -->
                <div class="help-section">
                    <div class="help-title">
                        <i class="fas fa-lightbulb"></i>
                        What you can try
                    </div>
                    <ul class="help-list">
                        <li class="help-item">Refresh the page and try again</li>
                        <li class="help-item">Check your internet connection</li>
                        <li class="help-item">Verify the selected month is valid</li>
                        <li class="help-item">Contact support if the issue persists</li>
                    </ul>
                </div>

                <a href="/" class="btn btn-primary">
                    <i class="fas fa-redo"></i>
                    <span>Try Again</span>
                </a>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <div class="footer-text">
                <i class="fas fa-headset"></i>
                Need help? Contact our support team
            </div>
            <div class="security-badge">
                <i class="fas fa-envelope"></i>
                support@company.com
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Enhanced accessibility
        document.addEventListener('keydown', function (e) {
            if (e.key === 'Enter' && e.ctrlKey) {
                window.location.href = '/';
            }
            if (e.key === 'Escape') {
                window.location.href = '/';
            }
        });

        // Focus management for accessibility
        document.addEventListener('DOMContentLoaded', function () {
            // Focus on the error card for screen readers
            document.querySelector('.form-card').setAttribute('tabindex', '-1');
        });
    </script>
</body>

</html>